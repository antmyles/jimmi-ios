from pathlib import Path

root = Path('/home/ubuntu/jimmi-fit-recovery')

onboarding = root / 'client/src/pages/Onboarding.tsx'
text = onboarding.read_text()
text = text.replace('  fitnessGoals: string[];\n  additionalInfo: string;\n};', '  fitnessGoals: string[];\n  targetWeight: string;\n  additionalInfo: string;\n};')
text = text.replace('  fitnessGoals: [],\n  additionalInfo: "",\n};', '  fitnessGoals: [],\n  targetWeight: "",\n  additionalInfo: "",\n};')
text = text.replace('const steps = ["Basics", "Activity", "Fitness", "Goal", "Nutrition", "Health", "Review"];', 'const steps = ["Basics", "Activity", "Fitness", "Goal", "Nutrition", "Health", "Review"];\nconst weightLossGoal = "Lose weight";')
text = text.replace('function calculateBirthdayAge(birthday: string, today = new Date()) {', 'function parseWeightValue(value: string) {\n  const parsed = Number(value);\n  return Number.isFinite(parsed) ? parsed : null;\n}\n\nfunction calculateBirthdayAge(birthday: string, today = new Date()) {')
text = text.replace('    weight: Number(form.weight) || 0,\n    height: form.height.trim(),', '    weight: Number(form.weight) || 0,\n    targetWeight: form.targetWeight ? Number(form.targetWeight) : null,\n    height: form.height.trim(),')
text = text.replace('function MultiSelectDropdown({ value, options, onChange, placeholder }: { value: string[]; options: string[]; onChange: (next: string[]) => void; placeholder: string }) {\n  const toggle = (option: string) => {\n    if (option === "None") return onChange(value.includes("None") ? [] : ["None"]);\n    const withoutNone = value.filter((item) => item !== "None");\n    onChange(withoutNone.includes(option) ? withoutNone.filter((item) => item !== option) : [...withoutNone, option]);\n  };', 'function MultiSelectDropdown({ value, options, onChange, placeholder }: { value: string[]; options: string[]; onChange: (next: string[]) => void; placeholder: string }) {\n  const detailsRef = useRef<HTMLDetailsElement>(null);\n  const toggle = (option: string) => {\n    if (option === "None") {\n      onChange(value.includes("None") ? [] : ["None"]);\n      detailsRef.current?.removeAttribute("open");\n      return;\n    }\n    const withoutNone = value.filter((item) => item !== "None");\n    onChange(withoutNone.includes(option) ? withoutNone.filter((item) => item !== option) : [...withoutNone, option]);\n  };')
text = text.replace('<details className="group rounded-2xl border border-white/10 bg-white/[0.035]" data-nutrition-control="compact-multi-select-dropdown">', '<details ref={detailsRef} className="group rounded-2xl border border-white/10 bg-white/[0.035]" data-nutrition-control="compact-multi-select-dropdown" data-none-autocollapse="true">')
text = text.replace('            fitnessGoals: Array.isArray(parsed.fitnessGoals) ? parsed.fitnessGoals : parsed.fitnessGoal ? [parsed.fitnessGoal] : [],\n          });', '            fitnessGoals: Array.isArray(parsed.fitnessGoals) ? parsed.fitnessGoals : parsed.fitnessGoal ? [parsed.fitnessGoal] : [],\n            targetWeight: parsed.targetWeight ? String(parsed.targetWeight) : "",\n          });')
text = text.replace('      fitnessGoals: splitList(profileQuery.data.fitnessGoals),\n      additionalInfo: profileQuery.data.additionalInfo ?? "",', '      fitnessGoals: splitList(profileQuery.data.fitnessGoals),\n      targetWeight: profileQuery.data.targetWeight ? String(profileQuery.data.targetWeight) : "",\n      additionalInfo: profileQuery.data.additionalInfo ?? "",')
old = '''  const update = <K extends keyof OnboardingForm>(key: K, value: OnboardingForm[K]) => {\n    setForm((current) => {\n      if (key === "gender" && value === "Male") {\n        return { ...current, [key]: value, healthComplications: current.healthComplications.filter((item) => item !== pregnancyHealthOption) };\n      }\n      return { ...current, [key]: value };\n    });\n    setErrors((current) => ({ ...current, [key]: "" }));\n  };'''
new = '''  const update = <K extends keyof OnboardingForm>(key: K, value: OnboardingForm[K]) => {\n    setForm((current) => {\n      let next: OnboardingForm = { ...current, [key]: value };\n      if (key === "gender" && value === "Male") {\n        next = { ...next, healthComplications: current.healthComplications.filter((item) => item !== pregnancyHealthOption) };\n      }\n      if (key === "fitnessGoals" && Array.isArray(value) && !value.includes(weightLossGoal)) {\n        next = { ...next, targetWeight: "" };\n      }\n      const nextErrors = collectStepErrors(step, next);\n      setErrors(nextErrors);\n      return next;\n    });\n  };'''
text = text.replace(old, new)
old = '''  const updateFoodAllergies = (nextFoodAllergies: string[]) => {\n    setForm((current) => ({\n      ...current,\n      foodAllergies: nextFoodAllergies,\n      otherFoodAllergy: nextFoodAllergies.includes("Other") ? current.otherFoodAllergy : "",\n    }));\n    setErrors((current) => ({ ...current, foodAllergies: "", otherFoodAllergy: "" }));\n  };'''
new = '''  const updateFoodAllergies = (nextFoodAllergies: string[]) => {\n    setForm((current) => {\n      const next = {\n        ...current,\n        foodAllergies: nextFoodAllergies,\n        otherFoodAllergy: nextFoodAllergies.includes("Other") ? current.otherFoodAllergy : "",\n      };\n      setErrors(collectStepErrors(step, next));\n      return next;\n    });\n  };'''
text = text.replace(old, new)
old = '''  const updateHealthComplications = (nextHealthComplications: string[]) => {\n    setForm((current) => {\n      const allowedHealthComplications = current.gender === "Male" ? nextHealthComplications.filter((item) => item !== pregnancyHealthOption) : nextHealthComplications;\n      return {\n        ...current,\n        healthComplications: allowedHealthComplications,\n        otherHealthComplication: allowedHealthComplications.includes("Other") ? current.otherHealthComplication : "",\n      };\n    });\n    setErrors((current) => ({ ...current, healthComplications: "", otherHealthComplication: "" }));\n  };'''
new = '''  const updateHealthComplications = (nextHealthComplications: string[]) => {\n    setForm((current) => {\n      const allowedHealthComplications = current.gender === "Male" ? nextHealthComplications.filter((item) => item !== pregnancyHealthOption) : nextHealthComplications;\n      const next = {\n        ...current,\n        healthComplications: allowedHealthComplications,\n        otherHealthComplication: allowedHealthComplications.includes("Other") ? current.otherHealthComplication : "",\n      };\n      setErrors(collectStepErrors(step, next));\n      return next;\n    });\n  };'''
text = text.replace(old, new)
old = '''  const validateStep = (targetStep = step) => {\n    const nextErrors: Record<string, string> = {};\n    if (targetStep === 0) {\n      if (form.name.trim().length < 2) nextErrors.name = "First name must be at least 2 characters.";\n      if (!form.birthday) nextErrors.birthday = "Date of birth is required.";\n      else if (calculateBirthdayAge(form.birthday) === null) nextErrors.birthday = "Enter a valid date of birth.";\n      else if ((calculateBirthdayAge(form.birthday) ?? 0) < 8) nextErrors.birthday = "You must be at least 8 years old to use JIMMI.";\n      if (!genderOptions.includes(form.gender.trim())) nextErrors.gender = "Select Male or Female.";\n      if (!form.weight.trim()) nextErrors.weight = "Weight is required.";\n      if (!form.height.trim()) nextErrors.height = "Height is required.";\n    }\n    if (targetStep === 1 && !form.activityLevel.trim()) nextErrors.activityLevel = "Activity level is required.";\n    if (targetStep === 2 && !form.fitnessLevel.trim()) nextErrors.fitnessLevel = "Fitness level is required.";\n    if (targetStep === 3 && form.fitnessGoals.length === 0) nextErrors.fitnessGoals = "Select at least one fitness goal.";\n    if (targetStep === 4) {\n      if (form.dietRestrictions.length === 0) nextErrors.dietRestrictions = "Select one dietary option.";\n      if (form.dietRestrictions.length > 1) nextErrors.dietRestrictions = "Choose only one dietary option.";\n      if (form.foodAllergies.length === 0) nextErrors.foodAllergies = "Select at least one allergy option.";\n      if (form.foodAllergies.includes("Other") && !form.otherFoodAllergy.trim()) nextErrors.otherFoodAllergy = "Please specify the other food allergy.";\n    }\n    if (targetStep === 5) {\n      if (form.healthComplications.length === 0) nextErrors.healthComplications = "Select at least one health complication option.";\n      if (form.healthComplications.includes("Other") && !form.otherHealthComplication.trim()) nextErrors.otherHealthComplication = "Please specify the other health complication.";\n    }\n    setErrors(nextErrors);\n    return Object.keys(nextErrors).length === 0;\n  };'''
new = '''  const collectStepErrors = (targetStep = step, source: OnboardingForm = form) => {\n    const nextErrors: Record<string, string> = {};\n    if (targetStep === 0) {\n      if (source.name.trim().length < 2) nextErrors.name = "First name must be at least 2 characters.";\n      if (!source.birthday) nextErrors.birthday = "Date of birth is required.";\n      else if (calculateBirthdayAge(source.birthday) === null) nextErrors.birthday = "Enter a valid date of birth.";\n      else if ((calculateBirthdayAge(source.birthday) ?? 0) < 8) nextErrors.birthday = "You must be at least 8 years old to use JIMMI.";\n      if (!genderOptions.includes(source.gender.trim())) nextErrors.gender = "Select Male or Female.";\n      if (!source.weight.trim()) nextErrors.weight = "Weight is required.";\n      else if (parseWeightValue(source.weight) === null) nextErrors.weight = "Enter a valid weight.";\n      if (!source.height.trim()) nextErrors.height = "Height is required.";\n    }\n    if (targetStep === 1 && !source.activityLevel.trim()) nextErrors.activityLevel = "Activity level is required.";\n    if (targetStep === 2 && !source.fitnessLevel.trim()) nextErrors.fitnessLevel = "Fitness level is required.";\n    if (targetStep === 3) {\n      if (source.fitnessGoals.length === 0) nextErrors.fitnessGoals = "Select at least one fitness goal.";\n      if (source.fitnessGoals.includes(weightLossGoal)) {\n        const currentWeight = parseWeightValue(source.weight);\n        const targetWeight = parseWeightValue(source.targetWeight);\n        if (!source.targetWeight.trim()) nextErrors.targetWeight = "Target weight is required when losing weight is a goal.";\n        else if (targetWeight === null) nextErrors.targetWeight = "Enter a valid target weight.";\n        else if (currentWeight !== null && targetWeight >= currentWeight) nextErrors.targetWeight = "Target weight must be less than your current weight.";\n      }\n    }\n    if (targetStep === 4) {\n      if (source.dietRestrictions.length === 0) nextErrors.dietRestrictions = "Select one dietary option.";\n      if (source.dietRestrictions.length > 1) nextErrors.dietRestrictions = "Choose only one dietary option.";\n      if (source.foodAllergies.length === 0) nextErrors.foodAllergies = "Select at least one allergy option.";\n      if (source.foodAllergies.includes("Other") && !source.otherFoodAllergy.trim()) nextErrors.otherFoodAllergy = "Please specify the other food allergy.";\n    }\n    if (targetStep === 5) {\n      if (source.healthComplications.length === 0) nextErrors.healthComplications = "Select at least one health complication option.";\n      if (source.healthComplications.includes("Other") && !source.otherHealthComplication.trim()) nextErrors.otherHealthComplication = "Please specify the other health complication.";\n    }\n    return nextErrors;\n  };\n\n  const validateStep = (targetStep = step) => {\n    const nextErrors = collectStepErrors(targetStep, form);\n    setErrors(nextErrors);\n    return Object.keys(nextErrors).length === 0;\n  };'''
text = text.replace(old, new)
text = text.replace('      weight: Number(form.weight),\n      height: form.height.trim(),', '      weight: Number(form.weight),\n      targetWeight: form.targetWeight ? Number(form.targetWeight) : null,\n      height: form.height.trim(),')
text = text.replace('<Field label="Weight" error={errors.weight} hint="Enter weight in pounds for now."><TextInput inputMode="numeric" value={form.weight} onChange={(event) => update("weight", event.target.value)} placeholder="185" /></Field>', '<Field label="Weight" error={errors.weight} hint="Enter weight in pounds for now."><TextInput inputMode="numeric" value={form.weight} onChange={(event) => update("weight", event.target.value)} onBlur={() => setErrors(collectStepErrors(step, form))} placeholder="185" /></Field>')
text = text.replace('<Field label="First name" error={errors.name}><TextInput value={form.name} onChange={(event) => update("name", event.target.value)} placeholder="Alex" minLength={2} /></Field>', '<Field label="First name" error={errors.name}><TextInput value={form.name} onChange={(event) => update("name", event.target.value)} onBlur={() => setErrors(collectStepErrors(step, form))} placeholder="Alex" minLength={2} /></Field>')
text = text.replace('<Field label="Date of Birth" error={errors.birthday} hint="JIMMI stores this securely to keep your profile age current automatically. You must be at least 8 years old."><TextInput type="date" value={form.birthday} onChange={(event) => update("birthday", event.target.value)} placeholder={birthdaySample} max={minimumBirthday} /></Field>', '<Field label="Date of Birth" error={errors.birthday} hint="JIMMI stores this securely to keep your profile age current automatically. You must be at least 8 years old."><TextInput type="date" value={form.birthday} onChange={(event) => update("birthday", event.target.value)} onBlur={() => setErrors(collectStepErrors(step, form))} placeholder={birthdaySample} max={minimumBirthday} /></Field>')
text = text.replace('<Field label="Fitness goal" error={errors.fitnessGoals}><MultiChoiceCardGrid value={form.fitnessGoals} onChange={(value) => update("fitnessGoals", value)} options={goalOptions} columns="sm:grid-cols-2 lg:grid-cols-4" /></Field>', '<Field label="Fitness goal" error={errors.fitnessGoals}><MultiChoiceCardGrid value={form.fitnessGoals} onChange={(value) => update("fitnessGoals", value)} options={goalOptions} columns="sm:grid-cols-2 lg:grid-cols-4" /></Field>\n                {form.fitnessGoals.includes(weightLossGoal) ? (\n                  <Field label="Target weight" error={errors.targetWeight} hint="Enter a goal weight in pounds. It must be lower than the current weight you entered in Basic details.">\n                    <TextInput inputMode="numeric" value={form.targetWeight} onChange={(event) => update("targetWeight", event.target.value)} onBlur={() => setErrors(collectStepErrors(step, form))} placeholder="170" />\n                  </Field>\n                ) : null}')
text = text.replace('["First name", form.name], ["Gender", form.gender], ["Weight", form.weight ? `${form.weight} lb` : "—"], ["Height", form.height], ["Activity", form.activityLevel], ["Fitness", form.fitnessLevel], ["Goals", form.fitnessGoals.join(", ")],', '["First name", form.name], ["Gender", form.gender], ["Weight", form.weight ? `${form.weight} lb` : "—"], ["Target weight", form.targetWeight ? `${form.targetWeight} lb` : "—"], ["Height", form.height], ["Activity", form.activityLevel], ["Fitness", form.fitnessLevel], ["Goals", form.fitnessGoals.join(", ")],')
onboarding.write_text(text)

schema = root / 'drizzle/schema.ts'
text = schema.read_text()
text = text.replace('  weight: int("weight").notNull(),\n  height: varchar("height", { length: 20 }).notNull(),', '  weight: int("weight").notNull(),\n  targetWeight: int("targetWeight"),\n  height: varchar("height", { length: 20 }).notNull(),')
schema.write_text(text)

routers = root / 'server/routers.ts'
text = routers.read_text()
text = text.replace('const onboardingInput = z.object({\n  firstName:', 'const onboardingInput = z.object({\n  firstName:')
text = text.replace('  weight: z.coerce.number().int().min(50, "Enter a realistic weight.").max(700, "Enter a realistic weight."),\n  height:', '  weight: z.coerce.number().int().min(50, "Enter a realistic weight.").max(700, "Enter a realistic weight."),\n  targetWeight: z.coerce.number().int().min(50, "Enter a realistic target weight.").max(700, "Enter a realistic target weight.").nullable().optional(),\n  height:')
text = text.replace('  additionalInfo: z.string().trim().max(1200).optional().nullable(),\n});', '  additionalInfo: z.string().trim().max(1200).optional().nullable(),\n}).superRefine((input, ctx) => {\n  if (input.fitnessGoals.includes("Lose weight")) {\n    if (input.targetWeight == null) {\n      ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["targetWeight"], message: "Target weight is required when losing weight is a goal." });\n    } else if (input.targetWeight >= input.weight) {\n      ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["targetWeight"], message: "Target weight must be less than your current weight." });\n    }\n  }\n});')
text = text.replace('    weight: input.weight,\n    height: input.height,', '    weight: input.weight,\n    targetWeight: input.fitnessGoals.includes("Lose weight") ? input.targetWeight ?? null : null,\n    height: input.height,')
text = text.replace('    `Weight: ${profile.weight} lb`,\n    `Height: ${profile.height}`,', '    `Weight: ${profile.weight} lb`,\n    `Target weight: ${profile.targetWeight ? `${profile.targetWeight} lb` : "not set"}`,\n    `Height: ${profile.height}`,')
routers.write_text(text)

db = root / 'server/db.ts'
text = db.read_text()
text = text.replace('      weight: values.weight,\n      height: values.height,', '      weight: values.weight,\n      targetWeight: values.targetWeight ?? null,\n      height: values.height,')
db.write_text(text)
